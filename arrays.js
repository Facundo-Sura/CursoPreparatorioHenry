/*var listaDeCompras = [];

//Lista de compras
listaDeCompras[3] = 'Tomates';
listaDeCompras[1] = 'Lechuga';

//Ver lista de compras
var elementoDelArray = listaDeCompras[1];
console.log(elementoDelArray);

//Lenght
var nombres = ['Matias', 'Maria', 'Diego', 'Rosa'].length;
console.log(nombres);

var colores = ['amarillo', 'azul'];
colores.push('rojo');
colores.unshift('verde');
colores.pop();
colores.shift();
console.log(colores);

// INCLUDES
var pintores = ['Picasso', 'Velázquez', 'Van Gogh', 'Dalí'];
var existeDali = pintores.includes('Dalí');
console.log(existeDali);

// EVERY
var numeros = [ 1, 6, 8, 9, 43 ];
var cumplenCondicion = numeros.every( ( num ) => { num > 5 } );
console.log(cumplenCondicion);

var palabra = 'Henri';
var palabraSeprada = palabra.split('');//indica al metodo que queremos que separe la palabra por cada caracter
palabraSeprada.pop();
palabraSeprada.push('y');
//console.log(palabraSeprada);
var palabraArreglada = palabraSeprada.join('');
console.log(palabraArreglada);
var numeros = [1, 2, 3, 4];
//numeros.forEach((num) => console.log(num));
//numeros.forEach(num => {if(num === 3) {console.log(num)}});
var masUno = numeros.map(num => {return num +1});
console.log(masUno);

var arr = [1, 2, 3, 4, 5];
for (var i = 0; i <  arr.length; i++) {
    console.log (arr[i]);
}*/

/*function encontrarLetraP(string) {
    var letras = string.split('');

    for (let i = 0; i < letras.length; i++) {
        if (letras[i] === 'p') {
            console.log('Si contiene a P');
        }
    }
}

encontrarLetraP('JavaScript');
encontrarLetraP('Henrry');*/

/*var arr = [];
while (arr.length < 5) {
    arr.push('BOOM');
}

console.log(arr);*/

//Bucle infinito
/*var arr = [];
var n = 1;

while (n < 3) {
    arr.push(Math.random());
}*/